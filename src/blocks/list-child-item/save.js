/**
 * WordPress dependencies
 */
import { InnerBlocks } from '@wordpress/block-editor';

/**
 * The save function for the List Item block.
 *
 * @since x.x.x
 * @return {Element} Element to render.
 */
const Save = () => (
	<InnerBlocks.Content />
);

export default Save;