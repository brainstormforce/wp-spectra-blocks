/**
 * External dependencies.
 */
import { InnerBlocks } from '@wordpress/block-editor';

/**
 * The save component for the slider-child block.
 *
 * @return {Element} The rendered block.
 */
const Save = () => (
	<InnerBlocks.Content />
);

export default Save;
