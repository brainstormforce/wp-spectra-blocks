/**
 * The 'Save' function.
 *
 * Returns null since this is a dynamic block rendered via PHP controller.
 *
 * @since x.x.x
 * @return {null} Null - block is rendered server-side.
 */
const Save = () => null;

export default Save;
